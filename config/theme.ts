const theme = {
  shadows: {
    outline: "none",
  },
  styles: {
    global: {
      body: {
        fontFamily: `"Lato", sans-serif`,
        minWidth: 300,
        margin: 10,
      },
    },
    config: {
      useSystemColorMode: true,
      initialColorMode: "dark",
    },
  },
};

export default theme;
